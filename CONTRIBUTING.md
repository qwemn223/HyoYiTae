# 데이콘 관련 규칙

### 🚀 해커톤 프로젝트 팀 그라운드 룰 (Ground Rules)

우리 팀의 원활한 협업과 불필요한 번아웃 방지, 그리고 9/17(목) 완성을 위한 4가지 공통 개발 규칙입니다.

---

#### 1. 재현성 보장 (랜덤 시드 42 고정)

- **규칙:** 모든 코드 상단에 `random_state = 42`처럼 랜덤 시드를 무조건 동일한 숫자로 고정합니다.
- **이유:** 시드를 고정하지 않으면 코드 내용이 같아도 실행할 때마다 점수가 달라져, 점수 상승이 피처/모델 변경 때문인지 단순 운인지 구분할 수 없게 됩니다.

#### 2. 실험 로그(Experiment Log) 의무화

- **규칙:** 어떤 모델(트리 모델, 선형 회귀, 딥러닝 등)을 사용하든 상관없이, 코드를 돌린 후 노션 실험 표에 한 줄로 무조건 기록합니다.
- **이유:** 각자 자유롭게 모델을 탐색하더라도 무엇을 바꿨을 때 점수가 올랐는지 명확히 추적할 수 있으며, 이 기록 자체가 18일 발표 PPT(성능 개선 과정)의 핵심 증빙 자료가 됩니다.

#### 3. 데이콘 제출 기준 통일 (로컬 CV 우선주의)

- **규칙:** 모델 종류와 무관하게, 로컬 5-Fold CV MAE 점수가 기존 최고 기록보다 개선된 결과물만 데이콘 리더보드에 제출합니다.
- **이유:** 데이콘 퍼블릭 점수에만 모델을 맞추다 보면 과적합이 발생해 최종 순위에서 미끄러지기 쉽습니다. 자체 검증 점수를 기준으로 삼아야 안전합니다.

#### 4. Git 협업 및 브랜치 규칙

- **규칙:**
    - `main` 브랜치에 직접 푸시(Commit & Push) 금지, `feature/기능명` 브랜치에서 작업 후 PR을 통해 머지합니다.
    - 데이터 파일(`train.csv`, `test.csv` 등)은 `.gitignore`에 추가하여 깃허브에 절대 올리지 않습니다.
- **이유:** 대용량 파일로 인한 레포 용량 초과를 방지하고, 코드 충돌(Conflict)로 인한 불필요한 작업 지연을 막습니다.